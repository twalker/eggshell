{
  "name": "eggshell",
  "version": "0.0.1",
  "private": true,
  "dependencies": {
    "express": "3.0.5",
    "jade": "*",
    "stylus": "*",
    "nib": "*",
    "livereload": "~0.3.0"
  },
  "devDependencies": {
    "mocha": "~1.6.0",
    "chai": "~1.3.0"
  },
  "scripts": {
    "test": "mocha --ui bdd --recursive --reporter spec --timeout 10000 --slow 300",
    "start": "node app"
  }
}
